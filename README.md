# Things to edit when adding an API route:
* Swagger documentation
* Param checker middleware
* Access control middleware (if needed)

# Things to edit when adding a new data type
* Swagger documentation
* Sequelize model
* GraphQL type/resolvers
